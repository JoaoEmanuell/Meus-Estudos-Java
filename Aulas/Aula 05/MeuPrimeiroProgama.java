public class MeuPrimeiroProgama {

    public static void main(String[] args) {
        //var number1 = 10;
        /*
        Váriaveis -> tipo: Texto = String (str)
        Váriaveis -> tipo: Números = (int)
        Váriaveis -> tipo: Números fracionados = (double)
        Váriaveis -> tipo: Boleanas = (verdadeiro | falso)
         */
        /*
        String login = "Olá"; //Representação de Texto (ASCII)
        int number1 = 10; //Representação de Números inteiros
        double number2 = 10.5; //Representação de Números reais
        boolean isDrive = false; //Representação boleana (Verdadeiro ou Falso)

        System.out.println(login);
        System.out.println(number1);
        System.out.println(number2);
        System.out.println(isDrive);
        */
        /*
        Váriaveis são espaçõs de mémoria alocado (representados por tipos)
         */
        /*
        int number; //Declaração
        number = 55;
        System.out.println(number);
        */
        /*
        Código para teste de if e else
        int age;
        String name;
        boolean isDrive;

        age = 12;
        name = "Emanuel";
        if (age < 18){
            isDrive = false;
            System.out.println(name + " tem " + age + " anos e não pode dirigir!");
        }else if (age > 18){
            isDrive = true;
            System.out.println(name + " tem " + age + " anos e pode dirigir!");
        }
    */
    }
}